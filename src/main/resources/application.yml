logging:
  level:
    root: INFO
  file:
    name: ${user.dir}/logs/java.log
server:
  port: 80
hello: Spring
dbAdapter:

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/card_db
    username: root
    password: roottest
  jpa:
    database: mysql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
